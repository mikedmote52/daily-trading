# Environment Variables for Explosive Stock Discovery System
# ========================================================

# REQUIRED: Polygon API Key for market data
# Get your free API key at: https://polygon.io/
POLYGON_API_KEY=your_polygon_api_key_here

# OPTIONAL: Advanced features (uncomment if using)
# ALPACA_API_KEY=your_alpaca_api_key_here
# ALPACA_SECRET_KEY=your_alpaca_secret_key_here
# CLAUDE_API_KEY=your_anthropic_api_key_here

# Production Environment Settings
ENVIRONMENT=production
NODE_ENV=production

# Database (auto-configured by Render)
# DATABASE_URL=postgresql://user:password@host:port/database
# REDIS_URL=redis://host:port

# API Configuration
PORT=8000

# Frontend Configuration
VITE_API_URL=https://explosive-discovery-api.onrender.com
VITE_WS_URL=wss://explosive-discovery-api.onrender.com

# Security (for production only)
# CORS_ORIGINS=https://yourdomain.com
# JWT_SECRET=your_jwt_secret_here

# Performance Tuning
# MAX_WORKERS=2
# TIMEOUT_SECONDS=60
# CACHE_TTL=300

# Portfolio Optimization (October 2025 - Replicate +63.8% monthly returns)
INITIAL_CAPITAL=1500
POSITION_SIZE_USD=100
MAX_POSITIONS=15
STOP_LOSS_PCT=0.15
TRAILING_STOP_PCT=0.10

# Stealth Detection (VIGL/CRWV/AEVA Calibrated)
# These values are optimized for pre-explosion detection based on historical winners
# VIGL: +324% (detected at RVOL 1.8x, +0.4% price change)
# CRWV: +171% (detected at RVOL 1.9x, -0.2% price change)
# AEVA: +162% (detected at RVOL 1.7x, +1.1% price change)
MIN_STEALTH_RVOL=1.5      # Minimum RVOL for accumulation detection
MAX_STEALTH_RVOL=2.0      # Maximum RVOL for stealth (exclude already-discovered stocks)
MAX_STEALTH_CHANGE=2.0    # Maximum daily price change % (detect before explosion)
MIN_PRICE=5.0             # Minimum price to avoid penny stocks

# Performance Targets
TARGET_MONTHLY_RETURN=0.638   # 63.8% monthly target (June-July baseline)
TARGET_WIN_RATE=0.933         # 93.3% win rate target (14/15 profitable)